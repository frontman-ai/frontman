# MCP Base Protocol And Versioning Traceability

This matrix covers the MCP `2026-07-28` base protocol, JSON-RPC messages, statelessness, schema usage, metadata, versioning and compatibility, extension negotiation, message-pattern overview, and custom transport rules. The architecture page contains no uppercase BCP 14 `MUST`, `MUST NOT`, `REQUIRED`, `SHOULD`, or `SHOULD NOT` statements; its descriptive constraints are represented by the normative rows below.

| Requirement ID | Normative text | Applicability | Code location | Positive test | Negative test | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| [BASE-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/index) | All implementations **MUST** support the base protocol, versioning, and message patterns. | Applicable within advertised scope | Shared contract; JavaScript/WordPress HTTP; browser HTTP/custom-Phoenix; Phoenix client | Protocol verifier, framework/WordPress endpoint suites, browser client/server suites, Phoenix channel suite | Malformed/direction suites plus optional-capability absence vectors | Implemented | Request/response is implemented. MRTR fulfillment and subscriptions are N/A because they are not advertised; exact absence evidence is indexed in `../capability-support.md`. |
| [RPC-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#messages) | All messages between MCP clients and servers **MUST** follow JSON-RPC 2.0. | Applicable | Shared/custom-Phoenix consumers; HTTP `FrontmanCore__MCP__RequestEnvelope` and `FrontmanCore__MCP__DecodedRequest` | Official/shared fixtures plus active HTTP request-envelope and decoded-boundary vectors | Wrong-version, malformed, response/notification direction, and mixed-discriminant vectors return HTTP 400/`-32600` | Implemented across supported transports | Metadata, method parameters, and active routing are validated in later boundary stages. |
| [RPC-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#requests) | Requests **MUST** include a string or integer ID. | Applicable | Shared `JsonRpc.Id` wire schema and `FrontmanCore__MCP__RequestEnvelope` classification/recovery | Empty/non-empty strings and both inclusive JavaScript safe-integer limits | Missing, null, fractional, both first unsafe integers, boolean, object, and array IDs | Implemented with documented local numeric limit | Unrestricted upstream integers outside the safe range are rejected because JavaScript cannot preserve their identity. |
| [RPC-003](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#requests) | A request ID **MUST NOT** be `null`. | Applicable | Shared `JsonRpc.Id` wire schema and `FrontmanCore__MCP__RequestEnvelope` | String and integral request IDs | Explicit missing/null-ID rejection in shared and HTTP envelope vectors | Implemented | Stricter than base JSON-RPC alone and aligned with MCP. |
| [RPC-004](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#requests) | A request ID **MUST NOT** match another request from the same sender that is still awaiting a response. | Applicable | `FrontmanClient__MCP__Client`; `FrontmanServer.MCPConnection` | Concurrent browser HTTP and Phoenix request tests | Duplicate/collision, late, cancelled, and terminal-ID tests | Implemented | Each issuing transport owns unique active correlation. |
| [RPC-005](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#result-responses) | Result responses **MUST** include the same ID as the corresponding request. | Applicable | Browser custom Phoenix dispatcher, browser HTTP client, and Phoenix connection owner | Shared string/numeric fixture and all three transport suites | Unknown, malformed, duplicate, late, cancelled, and cross-request IDs | Implemented | String and safe-integer identity is preserved end to end. |
| [RPC-006](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#result-responses) | Result responses **MUST** include a `result` field. | Applicable | Shared result schema and every HTTP/custom-Phoenix producer/consumer | Official/shared fixtures and transport result tests | Missing result and mixed result/error vectors | Implemented | A response resolves to exactly result or error. |
| [RPC-007](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#result-responses) | The `result` **MUST** include `resultType`. | Applicable | Shared generic/method schemas, browser result producers, Phoenix parser, and `FrontmanServer.Tasks.Interaction` persistence | Official complete fixtures, shared parity fixture, and focused persistence/replay tests | Producer/shared schemas reject omission; active clients separately normalize omission under RPC-010 compatibility | Implemented across runtime and persistence paths | Frontman-owned results emit `complete`; receiver normalization does not permit an emitter to omit the field. |
| [RPC-008](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#resulttype) | Supported `ResultType` values **MUST** comprise core values and values added by supported, capability-advertised extensions. | Applicable | Shared result schemas, browser HTTP client, and Phoenix connection parser/owner | Active browser and Phoenix tests accept valid `complete` and `input_required` shapes | Malformed `input_required` and unknown discriminators are rejected | Implemented | Valid `input_required` is recognized and surfaced without fulfillment or automatic retry. |
| [RPC-009](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#resulttype) | A `resultType` unrecognized by the client **MUST** be considered invalid. | Applicable | Browser HTTP/custom-Phoenix consumers and Phoenix connection owner | Recognized `complete`/`input_required` tests | Unknown-value and malformed-result rejection | Implemented | Unknown values are never coerced. |
| [RPC-010](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#resulttype) | For earlier-version compatibility, clients **MUST** treat absent `resultType` as `complete`. | Applicable client compatibility behavior | Browser response normalization and Phoenix `normalize_method_result` | Discovery, list, and call results without `resultType` normalize to `complete` in active clients | Explicit unknown discriminators remain invalid | Implemented | Normalization occurs before method-result validation and does not enable initialization-era negotiation. |
| [RPC-011](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#error-responses) | Error responses **MUST** include the same ID as the corresponding request, except when a malformed request prevents reading the ID. | Applicable | Shared envelopes; JavaScript/WordPress HTTP errors; browser/Phoenix dispatchers | Exact readable string/safe-integer error-ID tests | Missing, null, fractional, unsafe, boolean, object, and array IDs omit ID | Implemented | ID recovery validates only the ID and never accepts the malformed envelope. |
| [RPC-012](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#error-responses) | Error responses **MUST** include an `error` field containing `code` and `message`. | Applicable | `FrontmanProtocol__JsonRpc.Wire.errorResponseSchema` and named errors | Official modern error responses and arbitrary data fixtures | Complete required-field deletion, wrong-type, and mixed result/error vectors | Implemented | Open peer error fields and data remain lossless. |
| [RPC-013](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#error-responses) | Error codes **MUST** be integers. | Applicable | Shared generic integer schema and exact named-error codes | Standard, MCP-reserved, and maximum-safe integral code fixtures | Fractional, wrong-type, and wrong-named-code rejection | Implemented | Generic peer codes remain lossless while named modern errors enforce exact meanings. |
| [RPC-014](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#error-codes) | New codes **MUST NOT** be allocated in `-32000` through `-32019`, and new implementations **SHOULD NOT** use that sub-range at all. | Applicable | `FrontmanProtocol__MCP`; all current emitters | Named-error inventory and producer tests | Repository legacy-code scan and wrong-code mutations | Implemented | Removed Relay/legacy helpers no longer allocate this range. |
| [RPC-015](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#error-codes) | Except for legacy `-32002`, receivers **MUST NOT** assume a specific meaning for `-32000` through `-32019`. | Applicable | Generic shared error and browser/Phoenix consumers | Generic peer-error preservation tests | No local semantic mapping for unknown codes | Implemented | Unknown legacy-range values remain generic peer errors. |
| [RPC-016](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#error-codes) | Implementations **MUST NOT** emit undefined codes in `-32020` through `-32099` and **MUST** use defined codes only with their specified meanings. | Applicable | Named schemas and all HTTP/custom-Phoenix emitters | Inventory plus header/capability/version producer tests | Wrong-code and wrong-precedence vectors | Implemented | Only `-32020`, `-32021`, and `-32022` are emitted with defined meanings. |
| [RPC-017](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#error-codes) | Implementations of this version **MUST NOT** emit legacy codes `-32002` or `-32042`. | Applicable | Modern shared contract and current emitters | Reserved-code inventory | Repository/source and producer absence checks | Implemented | Legacy helper and Relay consumers are removed. |
| [RPC-018](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#error-codes) | Clients **SHOULD** still accept legacy resource-not-found code `-32002`. | Not applicable | None | None | None | Not applicable | Resources and legacy-version interoperability are outside the initial scope. |
| [RPC-019](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#error-codes) | New errors for purposes not defined by the specification **SHOULD** use codes outside JSON-RPC's reserved `-32768` through `-32000` range. | N/A: no Frontman-defined JSON-RPC error code | No extension error emitter | Standard/named error inventory | Inventory rejects unrecognized reserved allocations | N/A, absence tested | The execution-context extension uses specified standard MCP errors. |
| [RPC-020](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#notifications) | A notification receiver **MUST NOT** send a response. | Applicable to custom-Phoenix cancellation | Shared notification contract and browser cancellation dispatcher | Exact cancellation abort tests | No-response, malformed, unknown, completed, and late-ID tests | Implemented | Streamable HTTP uses transport close, not cancellation notifications. |
| [RPC-021](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#notifications) | Notifications **MUST NOT** include an ID. | Applicable to custom-Phoenix cancellation | Generic/cancellation schemas and browser custom-Phoenix receiver | Official/shared ID-less cancellation and exact abort fixtures | Runtime/generated schema rejects a notification carrying `id` | Implemented | Cancellation removes active correlation before aborting the matching signal. |
| [STATE-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#statelessness) | Servers **MUST NOT** rely on prior requests over the same connection to establish capabilities, protocol version, client identity, or other context. | Applicable | Browser custom Phoenix dispatcher and active framework endpoint validate each request | Browser vectors plus active framework call without discovery and per-request metadata/capability tests | Missing/incompatible metadata and spoofed client/session/resumption identity vectors | Implemented for custom Phoenix and active framework HTTP | Framework rate windows are authorization policy and convey no MCP context. |
| [STATE-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#statelessness) | Servers **SHOULD** be prepared to handle requests associated with multiple tasks, threads, or conversations. | Applicable | Browser handler and Phoenix connection owner | Interleaved multi-task and 100-call concurrency suites | Cross-task correlation/context-leak tests | Implemented | One connection owner handles multiple task observers. |
| [STATE-003](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#statelessness) | Servers **SHOULD NOT** require related operations to reuse the same connection or process. | Applicable | Framework/WordPress endpoints and browser durable execution handler | Call-without-discovery, reconnect, takeover, and recovery tests | Spoofed session/connection-affinity vectors | Implemented | Explicit durable context, not connection identity, owns cross-request work. |
| [STATE-004](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#statelessness) | Clients **SHOULD NOT** use an individual task, thread, or conversation as the lifetime boundary for a stdio process. | Not applicable | None | None | None | Not applicable | Frontman does not implement stdio. |
| [STATE-005](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#statelessness) | State spanning multiple requests **MUST** be referenced by an explicit identifier passed by the client on each request. | Applicable to custom Phoenix; HTTP has no protocol state | Execution-context schema, browser handler, Phoenix durable claim owner | Explicit task/tool-call ID and reconnect replay tests | Missing, empty, changed replay, stale generation, and connection-fallback tests | Implemented | PostgreSQL claim identity is durable authority. |
| [BASE-AUTH-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#auth) | Implementations using an HTTP transport **SHOULD** conform to the MCP authorization specification. | Applicable SHOULD; application authentication substitutes for MCP OAuth | JavaScript adapter authorization, WordPress session/capability/nonce boundary, and browser HTTP client | Framework and WordPress application-authorization suites | Focused hostile `resource_metadata` challenge test proves one `/mcp` POST and zero metadata, well-known, token, or registration requests | Unaccepted SHOULD deviation; absence focused-tested | Application authentication is implemented, but the HTTP transport does not conform to the MCP authorization profile. This still requires explicit acceptance or remediation. |
| [BASE-AUTH-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#auth) | Implementations using stdio **SHOULD NOT** follow the HTTP authorization specification and should retrieve credentials from the environment. | Not applicable | None | None | None | Not applicable | Frontman does not implement stdio. |
| [SCHEMA-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#schema-dialect) | Implementations **MUST** support JSON Schema 2020-12, including as the default when `$schema` is absent. | Applicable | Oracle verifier, browser `FrontmanClient__MCP__RemoteSchema`, and server `FrontmanServer.JSONSchema` | Pinned oracle plus browser and server default-2020-12 compilation and instance-validation tests | Unsupported and nested-dialect rejection on both runtime consumers | Implemented | Browser AJV and server JSV execute in interruptible isolated owners. |
| [SCHEMA-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#schema-dialect) | Implementations **SHOULD** document which additional schema dialects they support. | Applicable | `docs/mcp/implementation-limits.md`, browser remote validator, and server `FrontmanServer.JSONSchema` | Browser draft-07 and both default-2020-12 tests | Server rejects every explicit non-2020-12 dialect; browser rejects undeclared dialects | Implemented | Browser additionally supports draft-07; canonical server persistence accepts only 2020-12. |
| [SCHEMA-003](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#implementation-requirements) | Clients and servers **MUST** validate schemas according to their declared or default dialect. | Applicable | Browser remote validator and server `FrontmanServer.JSONSchema` | Valid input/output instances across every JSON root type | Malformed schemas and mismatched instances fail before model use or persistence | Implemented | Each compilation and validation operation has an isolated bounded owner. |
| [SCHEMA-004](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#implementation-requirements) | Clients and servers **MUST** handle unsupported dialects gracefully with an appropriate error indicating that the dialect is unsupported. | Applicable | Browser remote validator and server MCP catalog conversion | Supported explicit/default fixtures | Unsupported root or nested dialect excludes only the affected tool | Implemented | No downgrade or reinterpretation occurs. |
| [SCHEMA-005](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#schema-validation) | Schemas **MUST** be valid according to their declared or default dialect. | Applicable | Browser remote validator and server `FrontmanServer.JSONSchema` | Valid schema compilation and valid input/output instances | Malformed, unsupported, unresolved-reference, invalid-instance, and timeout paths | Implemented | Invalid catalog schemas exclude only the affected tool; invalid output resolves once as a canonical error. |
| [SCHEMA-006](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#ref-resolution) | Implementations **MUST NOT** automatically dereference `$ref` values that resolve to a network URI. | Applicable | Browser worker without a loader and server JSV build with no external resolver | Same-document `$ref` validation | HTTP, loopback, link-local, file, data, cross-document `$ref`, and `$dynamicRef` rejection | Implemented | No outbound resolution implementation is present on either runtime boundary. |
| [SCHEMA-007](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#ref-resolution) | Any opt-in mode that fetches non-local `$ref` values **MUST** be disabled by default. | Applicable | Browser and server validators have no production external resolver | Local-reference fixtures | External-reference corpus proves default rejection | Implemented | No opt-in resolver exists. |
| [SCHEMA-008](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#ref-resolution) | An opt-in remote `$ref` mode **SHOULD** enforce a host allowlist or reject loopback, link-local, and private addresses, and apply timeouts, size limits, and URI logging. | N/A: conditional feature absent | None | None | External-reference corpus proves no resolver exists | N/A, absence tested | A separately reviewed secure resolver is required before this optional mode can be enabled. |
| [SCHEMA-009](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#ref-resolution) | Schemas that fail validation because an external `$ref` is unresolved **SHOULD** be rejected rather than silently treated as permissive. | Applicable | Browser remote validator and server `FrontmanServer.JSONSchema` | Same-document reference fixture | External and cross-document references exclude the affected tool | Implemented | Neither validator fails open. |
| [SCHEMA-010](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#composition-keyword-resource-use) | Implementations **SHOULD** apply reasonable schema-composition bounds such as depth, subschema count, or a validation time budget. | Applicable | Browser and server schema validators plus `docs/mcp/implementation-limits.md` | Exact depth, container, and 100 ms vectors | One-beyond depth/container and 101 ms rejection | Implemented | Browser workers and server monitored processes are terminated after the immutable budget. |
| [META-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | Implementations **MUST NOT** make assumptions about values at MCP-reserved `_meta` keys. | Applicable | Open shared metadata schemas and all consumers | Unknown-valid metadata round trips | Malformed known-key and behavior-invariance tests | Implemented | Unknown reserved values are preserved or ignored, never policy inputs. |
| [META-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | If specified, an `_meta` prefix **MUST** be dot-separated labels followed by `/`; labels **MUST** start with a letter and end with a letter or digit and may contain only letters, digits, or interior hyphens. | Applicable | Shared metadata/extension grammar and `ExecutionContextExtension.identifier` | Valid-prefix corpus and exact `ai.frontman/execution-context` assertion | Malformed prefix, label, terminal-character, and newline corpus | Implemented | Applies directly to the Frontman execution-context extension. |
| [META-003](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | Implementations **SHOULD** use reverse-DNS notation for `_meta` prefixes. | Applicable | Shared `ai.frontman/execution-context` contract | Exact extension-name and documentation assertions | Generic nonconforming identifiers fail shared validation | Implemented | `ai.frontman/` is reverse DNS for Frontman's `frontman.ai` namespace. |
| [META-004](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | Unless empty, an `_meta` name **MUST** begin and end with an alphanumeric character. | Applicable | Shared metadata grammar and WordPress metadata validator | Valid-name corpus | Leading/trailing punctuation corpus | Implemented | Interior hyphens, underscores, dots, and alphanumerics are allowed. |
| [META-005](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | Every request **MUST** include `_meta.io.modelcontextprotocol/protocolVersion` and `_meta.io.modelcontextprotocol/clientCapabilities`. | Applicable | Shared request schemas, custom-Phoenix consumers, and route-independent HTTP `FrontmanCore__MCP__DecodedRequest` | Discovery/list/call runtime tests, shared fixture, and typed HTTP metadata acceptance | Shared deletion tests, browser invalid-params tests, and malformed/type-confused HTTP metadata vectors | Implemented for custom Phoenix and route-independent HTTP boundary | HTTP extraction preserves raw values until header comparison and supported-version classification complete, then parses the shared `RequestMeta` schema. |
| [META-006](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | A server **MUST** reject a request missing required per-request metadata with JSON-RPC `-32602`. | Applicable | Browser custom Phoenix dispatcher and route-independent HTTP `FrontmanCore__MCP__DecodedRequest` | Browser and HTTP complete-metadata request tests | Browser malformed request tests and HTTP missing/malformed metadata tests assert `-32602` | Implemented for custom Phoenix and route-independent HTTP boundary | Capability absence required by processing is separately classified as `-32021`. |
| [META-007](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | On HTTP, rejection for missing required request metadata **MUST** use `400 Bad Request`. | Applicable | Route-independent HTTP `FrontmanCore__MCP__ErrorResponse`; active adapters implemented | Typed metadata acceptance test | Missing client-capability metadata and malformed known-field tests assert HTTP 400 and `-32602` | Route-independent HTTP response implemented; active adapters implemented | Does not apply to the Phoenix envelope's status layer. |
| [META-008](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | Clients **SHOULD** include `io.modelcontextprotocol/clientInfo` on every request unless specifically configured not to do so. | Applicable | Browser HTTP request metadata and Phoenix request builder | Real-server and Phoenix emission inspection | Identity omission/configuration and spoofing behavior tests | Implemented | Identity is informational. |
| [META-009](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | A server **MUST NOT** rely on capabilities the client has not declared. | Applicable | Browser custom-Phoenix dispatcher and route-independent HTTP `FrontmanCore__MCP__DecodedRequest` | Declared required-capability paths | Missing/incompatible capability returns `-32021` in both boundaries | Implemented for custom-Phoenix extension and route-independent HTTP boundary | Capability checks are independent for every request; HTTP requirements are explicit processing inputs rather than global assumptions. |
| [META-010](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | If processing requires an undeclared client capability, the server **MUST** return `MissingRequiredClientCapabilityError` `-32021` with `data.requiredCapabilities` listing the missing capabilities. | Applicable | Shared named-error schema, browser custom Phoenix dispatcher, and route-independent HTTP `FrontmanCore__MCP__ErrorResponse` | Official fixture, open-capability vectors, browser tests, and HTTP declared-capability acceptance | Required capability deletion, malformed nested capability vectors, and HTTP absent/incompatible vectors | Implemented for required custom-Phoenix extension and route-independent HTTP boundary | No connection-history fallback is used; HTTP emits the caller-supplied exact aggregate requirement. |
| [META-011](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | On HTTP, `MissingRequiredClientCapabilityError` **MUST** use `400 Bad Request`. | Applicable when HTTP processing is configured with a required client capability | Route-independent HTTP `FrontmanCore__MCP__ErrorResponse`; active adapters compose that boundary but currently configure no nonempty required aggregate | Declared-capability HTTP-boundary acceptance | Absent and incompatible capability tests assert HTTP 400 and `-32021` | Implemented at the HTTP processing boundary; active condition currently absent | Custom Phoenix actively requires its execution-context extension and has transport-level `-32021` proof. Add an HTTP black-box vector when an active HTTP adapter configures a required client capability. |
| [META-012](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | Servers **SHOULD** include `io.modelcontextprotocol/serverInfo` in every result's `_meta` unless specifically configured not to do so. | Applicable | JavaScript/WordPress result builders and browser custom-Phoenix `resultMeta`/`withServerInfo` | JavaScript, WordPress, and browser discovery/list/call result identity tests | Existing call metadata is preserved while browser identity replaces any spoofed identity | Implemented | Every discovery, list, and call result includes the owning server identity. |
| [META-013](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | Implementations **SHOULD NOT** use self-reported `clientInfo` or `serverInfo` to change client or server behavior. | Applicable | All dispatchers/clients | Identity preservation/display tests | JavaScript and WordPress spoofed-identity invariance tests | Implemented | Version selection uses supported versions; behavior ignores identity. |
| [META-014](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | Implementations **SHOULD NOT** rely on self-reported `clientInfo` or `serverInfo` for security decisions. | Applicable | JavaScript authorization principal and WordPress authenticated principal | Principal-isolation tests | Spoofed identity cannot select principal or replace server identity | Implemented | Trusted transport/application authentication is the only authority. |
| [META-015](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | Notifications delivered through `subscriptions/listen` **MUST** include `io.modelcontextprotocol/subscriptionId` in `_meta`. | Not applicable | None | None | None | Not applicable | Subscriptions are explicitly excluded from the initial migration. |
| [META-016](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#meta) | When present, `traceparent`, `tracestate`, and `baggage` values **MUST** follow W3C Trace Context and W3C Baggage formats. | Applicable whenever reserved trace fields are present | Shared metadata schema plus Phoenix, framework, browser, and WordPress metadata validators | Ordinary vendor metadata remains accepted | Each reserved field is rejected at active request/result boundaries | Implemented by rejection | Frontman does not propagate traces, so reserved W3C fields are rejected rather than accepted without format support. |
| [ICON-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#icons) | Clients that render icons **MUST** support PNG and JPEG/JPG. | N/A: no icon renderer | No fetch/render owner | None | Tool metadata tests plus UI/network-path absence | N/A, absence tested | Frontman validates and preserves icon metadata but does not render it. |
| [ICON-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#icons) | Clients that render icons **SHOULD** also support SVG and WebP. | N/A: no icon renderer | No fetch/render owner | None | Tool metadata tests plus UI/network-path absence | N/A, absence tested | Becomes applicable only if Frontman renders MCP icons. |
| [ICON-003](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#icons) | Consumers of icon metadata **MUST** take appropriate security precautions and treat icon metadata and bytes as untrusted input. | N/A: no icon fetch/render consumer | No fetch/render owner | Metadata validation only | No icon network request, byte parser, or rendering path | N/A, absence tested | Merely preserving validated metadata does not make Frontman an icon consumer. |
| [ICON-004](https://modelcontextprotocol.io/specification/2026-07-28/basic/index#icons) | Clients **MUST** reject icon URIs using unsafe schemes and redirects. | N/A: no accepted renderable icons | No fetch/render owner | None | No icon fetch or redirect path | N/A, absence tested | A complete URI and redirect policy is required before rendering is introduced. |
| [VER-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning#protocol-version-negotiation) | A server receiving an unsupported protocol version **MUST** return `UnsupportedProtocolVersionError` `-32022` listing the versions it supports. | Applicable | Shared named schema and JavaScript/WordPress/custom-Phoenix dispatchers | Official/shared and all server unsupported-version tests | Malformed data and exact HTTP status vectors | Implemented | Only `2026-07-28` is supported. |
| [VER-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning#protocol-version-negotiation) | A client **SHOULD** select a mutually supported version from the error's `supported` list and retry, or surface an error if no compatible version exists. | Applicable, latest-only | Browser HTTP client and Phoenix connection owner | Same-version retry regression tests | No-mutual-version surfaced error and bounded-retry tests | Implemented | Never falls back to initialization-era versions. |
| [VER-003](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning#protocol-version-negotiation) | Servers **MUST** implement `server/discover`. | Applicable | Browser custom Phoenix dispatcher and route-independent framework dispatcher | Browser discovery tests, shared fixture, and schema-valid framework success with string/numeric IDs | Malformed metadata and unsupported-version requests reject before framework dispatch | Implemented for custom Phoenix and route-independent framework dispatch | Framework discovery advertises only core tools support; active `/mcp` routing is implemented. |
| [EXT-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning#extension-negotiation) | Extension identifiers **MUST** follow `_meta` key naming rules with a mandatory prefix. | Applicable | Shared generic extension grammar and exact execution-context identifier | Bilateral capability and request metadata fixtures use `ai.frontman/execution-context` | Unprefixed and malformed generic extension identifiers fail | Implemented | Applies to capabilities and extension metadata. |
| [EXT-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning#extension-negotiation) | If one party supports an extension and the other does not, the supporting party **MUST** revert to core behavior or reject the request with an appropriate error. | Applicable | Bilateral schemas and both custom-Phoenix consumers | Compatible version `1` advertisements and shared context fixture | Absent/incompatible server support fails locally; absent client support returns `-32021` | Implemented for custom Phoenix | No connection/session fallback exists; PostgreSQL claim identity provides durable ownership. |
| [EXT-003](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning#extension-negotiation) | Extensions **SHOULD** document their expected fallback behavior. | Applicable | `docs/mcp/frontman-execution-context-extension.md`; `../custom-phoenix-transport.md` | Version, scope, negotiation, errors, and no-fallback behavior documented | Independent ambiguity and fallback review | Extension documentation complete | Missing negotiation never falls back to connection or ACP session identity. |
| [COMPAT-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning#backward-compatibility-with-initialization-based-versions) | Clients interoperating with both protocol eras **SHOULD** cache era detection for the server process or HTTP origin. | Not applicable | None | None | None | Not applicable | Frontman explicitly does not implement dual-era or initialization-era interoperability. |
| [COMPAT-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning#backward-compatibility-with-initialization-based-versions) | A modern-only server **SHOULD** name its supported protocol versions in any error returned to an `initialize` request. | Deliberate SHOULD deviation | Active framework method dispatcher returns canonical method-not-found without method-specific data | Modern `server/discover` returns exact supported versions | `initialize` receives fixed `-32601` without reflecting request values or adding an undefined error-data shape | Accepted deviation | Frontman does not parse legacy initialization parameters. The canonical modern `MethodNotFoundError` has no standardized supported-version data field, so `server/discover` remains the sole version-discovery mechanism. |
| [COMPAT-003](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning#compatibility-matrix) | On stdio, modern clients facing legacy servers **SHOULD** send `server/discover` first to fail deterministically. | Not applicable | None | None | None | Not applicable | Frontman implements neither stdio nor legacy server interoperability. |
| [PATTERN-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/patterns/index) | Servers **MUST NOT** initiate JSON-RPC requests. | Applicable | Shared direction contract and all server dispatchers | Client-request/server-response suites | Server-request emission absence and inbound-response rejection | Implemented | MRTR production is absent; every result is complete or error. |
| [TRANS-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/index#messages) | JSON-RPC messages **MUST** be UTF-8 encoded. | Applicable | JavaScript/WordPress HTTP, browser HTTP, and Phoenix payload boundaries | Multibyte and byte-split UTF-8 suites | Malformed/truncated/depth/size rejection at each boundary | Implemented | Transport-specific measurement differences are documented in limits. |
| [TRANS-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/index#messages) | A binding **MUST** deliver client requests and notifications to the server and server responses and notifications to the client. | Applicable | Both Streamable HTTP roles and both custom-Phoenix peers | Real HTTP and Phoenix allowed-direction tests | Wrong-direction, dropped-owner, unknown-ID, and cross-request tests | Implemented for supported messages | HTTP client notifications are N/A; custom Phoenix supports cancellation notification. |
| [CTRANS-001](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/index#custom-transports) | Custom transports **MUST** preserve JSON-RPC format, message patterns, and the per-request metadata model. | Applicable | Browser `mcp:message` dispatcher and `FrontmanServer.MCPConnection` Phoenix owner | Shared cross-language fixture and focused browser/Phoenix contract tests | Malformed message, metadata, capability, context, direction, cancellation, and stale-owner vectors | Implemented | One Phoenix payload equals one JSON-RPC message; durable ownership and cancellation are independently fenced. |
| [CTRANS-002](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/index#custom-transports) | Custom transports **SHOULD** document connection establishment, message framing, and cancellation patterns. | Applicable | `../custom-phoenix-transport.md` | Both-peer contract suites and documentation review | Establishment/framing/direction/cancellation/teardown/replay/limit review | Implemented and documented | Authentication inheritance, ordering, delivery, replay, and backpressure are included. |
| [CTRANS-003](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/index#custom-transports) | Custom transports over a reliable bidirectional byte stream **SHOULD** reuse stdio newline-delimited framing. | Not applicable | None | None | None | Not applicable | Phoenix Channels provide message/event framing rather than a raw TCP or Unix-domain byte-stream binding. |
