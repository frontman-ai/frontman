Added Phoenix host-scoped `playgithub.localhost` authenticated route, SSL cert setup, tests, changeset, committed, pushed, and opened PR #1069.

Added PlayGithub string parser structs/routes for repo, tree, issues paths; printed parsed parts; tested auth, parsing, invalid issues.
