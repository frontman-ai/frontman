#!/bin/sh
set -eu

shared_env="${FRONTMAN_SHARED_ENV:-/opt/frontman/shared/discord.env}"

if [ ! -r "$shared_env" ]; then
  echo "ERROR: Missing shared environment file: $shared_env" >&2
  exit 1
fi

set -a
. "$shared_env"
set +a
unset shared_env
