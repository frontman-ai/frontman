---
"@frontman-ai/frontman-core": patch
---

Fix file discovery in projects without Git. Preserve nested directories in filesystem trees, include non-ignored untracked files in Git trees, and report fallback notices and source-root context.
