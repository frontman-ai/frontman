---
"@frontman-ai/astro": minor
---

Record the latest Astro client navigation's source URL, destination URL, and lifecycle phase during development. Guard lifecycle transitions against out-of-order completion events and capture the destination after the swap. Track hash-only navigation with a separate `hash-change` phase, including history traversal. Prevent duplicate navigation listeners and history wrappers when browser instrumentation initializes again.
