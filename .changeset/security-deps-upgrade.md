---
"@frontman/vite-plugin": patch
---

Resolve all critical and high severity Dependabot security alerts. Upgrade next.js to patched versions (16.1.6, 15.5.12), add root resolutions for 13 vulnerable transitive dependencies, remove stale lockfiles from yarn workspace members, deduplicate yarn.lock, and delete unused dogfooding app.
