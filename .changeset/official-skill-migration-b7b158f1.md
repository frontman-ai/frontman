---
"frontman": patch
---

Install the official design polish skill through a reversible data migration. Rollback removes only the migration's fixed UUID, preserving pre-existing catalog entries with different IDs. No seed scripts run during deployment.
