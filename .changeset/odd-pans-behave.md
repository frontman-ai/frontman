---
"@frontman-ai/client": patch
---

Update the server's ReqLLM dependency for experimental GPT-6 Astra Responses support. Pin LLMDB to the upstream commit with Astra metadata until a Hex release includes it. The model picker remains unchanged pending a live Codex access check.
