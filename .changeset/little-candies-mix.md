---
---

Mark stable prompt prefixes during composition and preserve block metadata through the existing Swarm message path. Delegate cache encoding to ReqLLM and OpenRouter instead of maintaining model-specific prompt handling.
