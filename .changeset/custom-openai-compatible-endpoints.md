---
"@frontman-ai/client": minor
---

Add Custom Providers to Settings so users can configure per-user, publicly reachable OpenAI-compatible providers. Each provider supports multiple model IDs and an optional API key, and appears as its own model-picker group once a model is configured. Outbound requests resolve and pin public destinations before connecting. Keyless providers use the placeholder key required by the request client.
