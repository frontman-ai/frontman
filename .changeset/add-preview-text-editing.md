---
"@frontman-ai/client": patch
---

Add inline text editing in the web preview: an "Edit text" mode lets you click any text element and rewrite it directly in the preview (preview-only). The edit is attached to your next chat message so the agent applies it in the corresponding source file.
