---
"marketing": patch
---

Fix homepage WebMCP registration under the production Content Security Policy. Compile validators at build time instead of generating browser code. Allow support submissions to the production API without enabling `unsafe-eval`. Document browser activation requirements.
